<Page
    x:Class="IsmDevice.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IsmDevice"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="stateTextBlock" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Margin="10,75,0,0" TextWrapping="Wrap" Text="state..." VerticalAlignment="Top"/>
            <ScrollViewer x:Name="scrollViewer" Grid.Row="1" Grid.Column="0" Height="500" Width="340" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Visible" >
                <RichTextBlock x:Name="console" Width="340" />
            </ScrollViewer>
        </Grid>


    </Grid>
</Page>
