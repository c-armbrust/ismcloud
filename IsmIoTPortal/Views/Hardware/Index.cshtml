@model IEnumerable<IsmIoTPortal.Models.Hardware>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Board)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Camera)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Board)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Camera)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HardwareId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HardwareId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HardwareId })
        </td>
    </tr>
}

</table>
